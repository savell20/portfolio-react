# Supabase — for the shared testimonial wall.
# Create a free project at https://supabase.com, then paste the values from
# Project Settings → API. Without these, the wall falls back to localStorage.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
